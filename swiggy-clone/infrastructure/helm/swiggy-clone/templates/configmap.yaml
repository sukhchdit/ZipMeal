apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "swiggy-clone.fullname" . }}-config
  labels:
    {{- include "swiggy-clone.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
